{
    "cloud":"${sap_cloud}",
    "hostname": "${sap_hostname}",
    "instance-id": "${sap_instance_id}",
    "state": "${sap_state}",
    "type": "${sap_type}",
    "method": "${sap_method}",
    "level": "${sap_level}",
    "account-id": "${sap_account_id}",
    "customer-id": "${sap_customer_id}",
    "system-id": "${sap_system_id}",
    "database-name": "${sap_database_name}",
    "database-id": "${sap_database_id}",
    "s3-path": "${sap_s3_path}",
    "timestamp-send": ${sap_timestamp_send},
    "timestamp-last-successful": ${sap_timestamp_last_successful},
    "timestamp-bkp-begin": ${sap_timestamp_bkp_begin},
    "timestamp-bkp-end": ${sap_timestamp_bkp_end},
    "backup-size": ${sap_backup_size},
    "dbtype": "${sap_dbtype}",
    "error-message": "${sap_error_message}",
    "script-version": "${sap_script_version}"
}
